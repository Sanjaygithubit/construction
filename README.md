# Stratum React + Tailwind

Single-file React website split into a clean Vite React project with Tailwind configured.

The form backend call has been removed. Enquiries are saved locally in the browser so the original success flow works without requiring any server.

## Run

`npm install`
`npm run dev`
